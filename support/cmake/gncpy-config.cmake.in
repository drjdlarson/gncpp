@PACKAGE_INIT@

if (NOT TARGET lager::gncpy)
  include(${CMAKE_CURRENT_LIST_DIR}/@targets_export_name@.cmake)
endif ()

# check_required_components(gncpy)
find_dependency(Eigen ${GNCPY_EIGEN_VERSION} REQUIRED)
find_dependency(Boost 1.82.0 REQUIRED)